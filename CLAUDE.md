# Inkhaven Tracker

Single-page React app (`src/App.jsx`) for tracking daily blog posts during April 2026.

## Changelog

The `CHANGELOG` constant at the top of `src/App.jsx` powers the in-app changelog modal. **Always update it with every commit** — add a new entry or append to an existing date's changes array.
